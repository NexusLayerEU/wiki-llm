{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities the WikiForge window needs.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
